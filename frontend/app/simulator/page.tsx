// Enter a purchase and tenure. Before/after on buffer, ratios, score and band.
// The reason the product exists. Calls POST /simulate. See docs/API-CONTRACT.md §4.

export default function SimulatorPage() {
  return (
    <main>
      <h1>Simulator</h1>
      <p>Not yet implemented.</p>
    </main>
  );
}
